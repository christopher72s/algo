// Copy your function `sentenceSplitter` from the first exercise here

export const sentenceSplitter = (word: string): string[] => {
  return word.split(" ");

  // Code your function sentenceSplitter here
};
